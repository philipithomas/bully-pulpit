interface:
  display_name: "Finalize Postcard"
  short_description: "Finalize the monthly Postcard from its PR"
  default_prompt: "Use $finalize-postcard to finalize the next monthly Postcard from its open PR while preserving every human comment detail and full quote."
