{
  "buildCommand": "pnpm content:check && pnpm db:migrate:deploy && pnpm build && (pnpm chroma:sync:deploy || echo 'chroma:sync failed (non-fatal); skipping search index update')",
  "crons": [
    { "path": "/api/cron/suppression-sync", "schedule": "0 * * * *" },
    { "path": "/api/cron/subscriber-backup", "schedule": "0 11 1 * *" }
  ]
}
