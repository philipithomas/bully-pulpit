/** Shared search behavior for Bell and other clients of the site's MCP tools. */
export const SEARCH_ALGORITHM_GUIDANCE = `Search favors keyword matches and, when available, combines them with semantic similarity using weighted reciprocal rank fusion. This combines positions in two ranked lists; result order is a relevance hint, not a confidence score or proof of a claim. Keyword matches can fill the result limit and leave related semantic-only sources out. Post keyword search first tries all query terms, broadening only when that returns no results; image keyword search also allows partial-word and spelling-tolerant matches. Extra query words can therefore change which sources appear.

The index includes titles, subtitles/descriptions, body text, image alt text, authored photo locations, and public camera metadata. Image embeddings include actual image content together with its descriptive text, so a photo can match a visible subject without naming it in the caption. An image URL in a result does not mean you have visually inspected it. Visual similarity cannot establish a precise location: confirm that from authored source metadata or text.

The default posts scope groups matching text chunks and images under their source post or page. The images scope ranks individual cover and body images; its excerpts contain image alt text or a title fallback rather than a quote from the post body. Several hits can belong to one post or show the same venue. Group those as one source or place when appropriate, and do not count them as separate visits. A body image does not inherit the cover photo's location or camera settings.

Each search returns at most ten relevance-ranked matches, not a complete inventory or a strict city/date filter. Check the returned locations and dates against the question. If results are sparse or off-topic, simplify to the distinctive name, try an alternative spelling or synonym, or search a missing aspect separately. Keyword search still works when semantic retrieval is unavailable, so do not assume every query received semantic matching. Stop refining once searches stop adding useful evidence.`
